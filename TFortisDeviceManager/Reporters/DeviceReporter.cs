using Serilog;
using TFortisDeviceManager.Properties;

namespace TFortisDeviceManager.Reporters
{
    public class DeviceReporter : ReporterBase
    {
       /* public void CreateDeviceError(string error) => AddError(string.Format(Resources.CreateDeviceError, error));
        public DeviceReporter(ILogger logger) : base(logger)
        {
        }*/
    }
}
